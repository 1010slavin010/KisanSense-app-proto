/* KisanSense design system — Phase 1 */

@import url('https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=Inter:wght@400;500;600;700&display=swap');

:root {
  --color-bg: #F5F3EC;
  --color-surface: #FFFFFF;
  --color-surface-alt: #FBFAF5;
  --color-border: #E3DFD1;
  --color-text: #23271F;
  --color-text-muted: #6E6C5E;

  --color-primary: #1F5C52;
  --color-primary-soft: #E4EEEA;

  --color-good: #4C7A5A;
  --color-good-soft: #E7EFE7;
  --color-warning: #B9791F;
  --color-warning-soft: #FBEEDD;
  --color-alert: #B4532F;
  --color-alert-soft: #F8E3D9;

  --font-display: 'Fraunces', Georgia, serif;
  --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;

  --radius-lg: 20px;
  --radius-md: 14px;
  --shadow-card: 0 1px 2px rgba(35, 39, 31, 0.05), 0 10px 24px rgba(35, 39, 31, 0.06);
}

html, body, [class*="css"] {
  font-family: var(--font-body);
  color: var(--color-text);
}

.stApp {
  background-color: var(--color-bg);
}

.block-container {
  padding-top: 2.5rem;
  padding-bottom: 4rem;
  max-width: 1080px;
}

footer { visibility: hidden; }
#MainMenu { visibility: hidden; }

/* Navbar */
.navbar-brand {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--color-primary);
  padding-top: 0.4rem;
}

.stButton > button {
  background-color: transparent;
  border: 1px solid transparent;
  border-radius: 999px;
  color: var(--color-text-muted);
  font-family: var(--font-body);
  font-weight: 500;
  padding: 0.45rem 1rem;
  transition: background-color 0.15s ease, color 0.15s ease;
}

.stButton > button:hover {
  background-color: var(--color-primary-soft);
  color: var(--color-primary);
  border-color: transparent;
}

.stButton > button:disabled {
  background-color: var(--color-primary);
  color: #FFFFFF;
  opacity: 1;
  border-color: var(--color-primary);
}

.navbar-divider {
  border: none;
  border-top: 1px solid var(--color-border);
  margin: 0.75rem 0 1.75rem 0;
}

/* Hero */
.hero-title {
  font-family: var(--font-display);
  font-size: 2.75rem;
  font-weight: 600;
  margin-bottom: 0.2rem;
  color: var(--color-text);
}

.hero-tagline {
  font-size: 1.05rem;
  color: var(--color-text-muted);
  margin-bottom: 0.9rem;
}

.status-dot-row {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background-color: var(--color-good);
  display: inline-block;
}

.status-dot-label {
  font-size: 0.9rem;
  color: var(--color-text-muted);
}

.section-spacer {
  height: 2.25rem;
}

/* Metric cards */
.metric-card {
  background-color: var(--color-surface);
  border: 1px solid var(--color-border);
  border-left: 4px solid var(--color-border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-card);
  padding: 1.5rem 1.5rem 1.25rem 1.4rem;
  height: 100%;
}

.metric-card-good { border-left-color: var(--color-good); }
.metric-card-warning { border-left-color: var(--color-warning); }
.metric-card-alert { border-left-color: var(--color-alert); }

.metric-card-title {
  font-size: 0.95rem;
  color: var(--color-text-muted);
  margin-bottom: 0.35rem;
}

.metric-card-value {
  font-family: var(--font-display);
  font-size: 2.1rem;
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 0.6rem;
}

.badge {
  display: inline-block;
  font-size: 0.8rem;
  font-weight: 500;
  padding: 0.25rem 0.7rem;
  border-radius: 999px;
  margin-bottom: 0.8rem;
}

.badge-good { background-color: var(--color-good-soft); color: var(--color-good); }
.badge-warning { background-color: var(--color-warning-soft); color: var(--color-warning); }
.badge-alert { background-color: var(--color-alert-soft); color: var(--color-alert); }

.metric-progress-track {
  width: 100%;
  height: 8px;
  border-radius: 999px;
  background-color: var(--color-surface-alt);
  border: 1px solid var(--color-border);
  overflow: hidden;
  margin-bottom: 0.9rem;
}

.metric-progress-fill {
  height: 100%;
  border-radius: 999px;
}

.metric-progress-good { background-color: var(--color-good); }
.metric-progress-warning { background-color: var(--color-warning); }
.metric-progress-alert { background-color: var(--color-alert); }

.metric-card-description {
  font-size: 0.9rem;
  color: var(--color-text-muted);
  line-height: 1.5;
  margin: 0;
}

/* Assistant */
.assistant-title {
  font-family: var(--font-display);
  font-size: 1.6rem;
  font-weight: 600;
  margin-bottom: 0.2rem;
}

.assistant-subtitle {
  color: var(--color-text-muted);
  margin-bottom: 1.1rem;
}

/* Placeholder pages */
.placeholder-card {
  background-color: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-card);
  padding: 3rem 2rem;
  text-align: left;
  max-width: 560px;
  margin-top: 1.5rem;
}

.placeholder-title {
  font-family: var(--font-display);
  font-size: 1.8rem;
  margin-bottom: 0.6rem;
}

.placeholder-description {
  color: var(--color-text);
  margin-bottom: 0.5rem;
  line-height: 1.5;
}

.placeholder-note {
  color: var(--color-text-muted);
  font-size: 0.9rem;
  margin: 0;
}

/* Chat bubbles */
[data-testid="stChatMessage"] {
  background-color: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 0.4rem 0.6rem;
  margin-bottom: 0.6rem;
}

/* Responsive */
@media (max-width: 640px) {
  .hero-title { font-size: 2.1rem; }
  .navbar-brand { font-size: 1.25rem; }
  .block-container { padding-top: 1.5rem; }
}
